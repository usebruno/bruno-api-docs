/** Delay before the variable hover card opens / closes on pointer hover (ms). */
export const HOVER_OPEN_MS = 50;
export const HOVER_CLOSE_MS = 500;

/** Gap between a floating overlay and its anchor (px). */
export const GAP = 8;
/** Minimum distance a floating overlay keeps from the viewport edges (px). */
export const VIEWPORT_MARGIN = 8;
